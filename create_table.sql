CREATE TABLE meteors(
       stamp   VARCHAR(255),
       status  VARCHAR(255),
       url     VARCHAR(255),
       address VARCHAR(255),
       agent   VARCHAR(255),
       referer VARCHAR(255),
       scene   VARCHAR(255),
       time    VARCHAR(255)
);
